---
import { twMerge } from "tailwind-merge";
import { focusRing, hoverSquare } from "lib/tailwind";

const links = [
  {
    title: "About",
    href: "/",
  },
  {
    title: "Projects",
    href: "/projects",
  },
  {
    title: "Uses",
    href: "/uses",
  },
];

const { pathname } = Astro.url;

function handleLinkClass(index: number, href: string) {
  const baseClasses = "px-3 py-1 rounded-lg transition hocus:scale-110";
  const isCurrent = pathname === href;

  const directionByIndex = {
    0: "top",
    1: "left",
    2: "bottom",
    3: "right",
  };

  const classByCurrent = {
    true: "bg-slate-200 dark:bg-slate-800 hocus:before:scale-0",
    false: "",
  };

  return twMerge(baseClasses, hoverSquare(directionByIndex[index]), focusRing, classByCurrent[isCurrent]);
}
---

<aside class="w-full sm:sticky sm:top-4">
  <nav class="flex flex-wrap items-center justify-between gap-3 sm:items-start sm:flex-col">
    <a class={twMerge("py-1.5 px-3 self-center rounded-lg transition", focusRing)} href="/">
      <img src="/favicon-dark.svg" class="w-12 h-12 sm:w-14 sm:h-14" alt="logo" />
    </a>
    <ul class="flex gap-3 sm:gap-2 sm:flex-col">
      {
        links.map(({ title, href }, index) => (
          <li class="flex justify-center">
            <a class={handleLinkClass(index, href)} href={href}>
              {title}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</aside>

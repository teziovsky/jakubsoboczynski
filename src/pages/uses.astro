---
import UsesView from "@/components/views/uses.astro";
import { languages } from "@/i18n/ui";

export async function getStaticPaths() {
  return Object.keys(languages).map((lang) => {
    return { params: { lang } };
  });
}
---

<UsesView />

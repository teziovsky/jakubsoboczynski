---
import AboutMeView from "@/pages/_components/about-me-view.astro";
import { languages } from "@/i18n/ui";

export async function getStaticPaths() {
  return Object.keys(languages).map((lang) => {
    return { params: { lang } };
  });
}
---

<AboutMeView />
